<?php  
    // Arithmetic operators
    /**
     * +
     * -
     * *
     * /
     * %
     */

     // Assignment operators
     /**
      * =
      * +=
      * -=
      * *=
      * /=
      * %=
      */

     $x = 2;
     $x += 3; 
     $x -= 2; 
     $x *= 3;
     $x /= 3; 
     $x %= 2;  
     echo $x."<br>";

    //   Logical operators
    /**
     * &&
     * ||
     * and
     * or
     * !
     */

    // Comparisons
    /**
     * ==
     * ===
     * !=
     * !==
     * >
     * <
     * >=
     * <=
     */

    // Operator precedence
    /**
     * ()
     * **
     * * / %
     * + -
     * = += -= *= /=
     * and or
     * && ||
     * == === != !== > < >= <=
     */

    echo (2 + 3)**2 * 2;
?>